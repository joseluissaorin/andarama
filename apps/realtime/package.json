{
  "name": "@andarama/realtime",
  "version": "0.1.0",
  "description": "Tiempo real de Andarama: Durable Objects (Cloudflare) y salas WS (self-host)",
  "license": "EUPL-1.2",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./durable-objects": "./src/durable-objects.ts",
    "./node": "./src/node.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "devDependencies": {
    "@types/ws": "^8.5.14",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10",
    "ws": "^8.18.0"
  }
}
